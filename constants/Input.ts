import { StyleSheet } from "react-native";

export const InputStyle = StyleSheet.create({
  container: {
    paddingHorizontal: 0,
    width: "auto",
  },
  inputContainer: {
    borderWidth: 1,
    color: "#222",
    borderRadius: 8,
    paddingHorizontal: 10,
  },
  labelStyle: {
    marginBottom: 5,
  },
});
